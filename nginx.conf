server {
    listen 80;
    server_name _;

    root /var/www/axis;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;

    # Service worker — must not be cached, needs Service-Worker-Allowed for root scope
    location = /sw.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        add_header Service-Worker-Allowed "/";
        add_header X-Content-Type-Options "nosniff";
        default_type application/javascript;
    }

    # Shared worker
    location = /bareworker.js {
        add_header Cache-Control "public, max-age=3600";
        default_type application/javascript;
    }

    # HTML files — short cache so updates arrive quickly
    location ~* \.html$ {
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # JS and CSS — cache with hash-busting in mind
    location ~* \.(css|js)$ {
        add_header Cache-Control "public, max-age=86400";
        default_type application/javascript;
    }

    # Images and fonts
    location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        add_header Cache-Control "public, max-age=604800";
        expires 7d;
    }

    # Everything else
    location / {
        try_files $uri $uri/ =404;
    }
}

# Redirect HTTP to HTTPS (uncomment when you have SSL)
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     # ... same location blocks as above ...
# }
